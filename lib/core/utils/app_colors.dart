import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const Color primaryRed = Color(0xFFDB3022);
  static const Color background = Color(0xFFF9F9F9);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF222222);
  static const Color grey = Color(0xFF9B9B9B);

  // ألوان الحالات (Status Colors)
  static const Color success = Color(0xFF2AA952); // الأخضر بتاع علامة الصح
  static const Color error = Color(0xFFF01F0E);   // الأحمر بتاع رسائل الخطأ
  static const Color saleHot = Color(0xFFDB3022); // لون التخفيضات الكبيرة

  // ألوان إضافية من التصميم
  static const Color shadowColor = Color.fromRGBO(211, 38, 38, 0.25); // ظل الزرار الأحمر
}